<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.HybridCleaner" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Theme.HybridCleaner" parent="Base.Theme.HybridCleaner"/>

    <style name="Theme.HybridCleaner.NoActionBar" parent="Base.Theme.HybridCleaner">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

</resources>